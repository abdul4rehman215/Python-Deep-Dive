# 📘 Interview Q&A – Lab 02: Data Types & Variables

1. What is a variable in Python?
   - A named reference used to store data in memory.

2. How does Python assign data types?
   - Dynamically based on the value assigned.

3. Which function checks a variable’s data type?
   - The type() function.

4. What is implicit type casting?
   - Automatic conversion of data types by Python.

5. Why did int + float return a float?
   - Python converts to float to avoid data loss.

6. What is explicit type casting?
   - Manual conversion using functions like int() or str().

7. Why is explicit casting important?
   - User input is usually received as strings.

8. Can Python variables change data types?
   - Yes, dynamically during execution.

9. What data type was used for membership status?
   - Boolean.

10. Where are data types heavily used in real-world apps?
    - User input validation, APIs, databases, and automation.
